<% user = comment.user %>
<div id="<%= dom_id comment %>">
  <p>
    <%= "#{capitalize_first_letter(user.name)} @#{user.username} #{time_ago_in_words(comment.updated_at)}" %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= comment.body %>
  </p>
</div>
